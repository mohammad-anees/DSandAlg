import React from 'react';

import { Box, Typography } from '@material-ui/core';

const HomePage: React.FC = () => {
  return (
    <Box margin='auto' px={2}>
      <Typography variant='h5'>Home Page</Typography>
    </Box>
  );
};

export default HomePage;
